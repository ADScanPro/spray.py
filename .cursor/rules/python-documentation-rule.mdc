---
alwaysApply: true
---
- Add module-level docstrings that summarise purpose and key public entry points.
- Document every public class and function using Google-style docstrings with `Args`, `Returns`, and `Raises` sections where applicable.
- Update inline documentation whenever behaviour or parameters change; do not leave stale examples.
- Include usage examples in docstrings when APIs are non-trivial (reference snippets under `docs/` if available).